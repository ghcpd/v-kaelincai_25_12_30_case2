# issue_project_fixed

Fixed version of the scientific calculator project addressing the negative number regression introduced in v1.1.

Run tests:

```powershell
pip install -r requirements.txt
pytest tests/ -v
```
